<div class="row row-cards">
  
  <div class="col-lg-8">
    <div class="card d-flex flex-column" style="min-height: 600px;">
      <div class="card-header">
        <h3 class="card-title">Catálogo de Productos</h3>
      </div>
      
      <div class="card-body border-bottom py-3">
      <div class="row">
        <div class="col-md-12">
          <div class="input-icon">
            <span class="input-icon-addon">
              <i class="ti ti-search"></i>
            </span>
            <input type="text" 
                   class="form-control" 
                   [(ngModel)]="buscarpro" 
                   (input)="onBuscarChange()"  
                   (input)="paginaActual = 1" 
                   placeholder="Buscar producto por nombre o código...">
          </div>  <!-- agregue onchange apra autocompletado, revisar lo de la calse mb-3 de tabler -->
        </div>
      </div>
    </div>
    <div class="position-relative mb-3">
  
   
  <!-- Dropdown de sugerencias -->
  @if (sugerencias.length > 0) {
    <ul class="list-group position-absolute w-100">
      @for (s of sugerencias; track s.id) {
        <li class="list-group-item list-group-item-action"
            (click)="seleccionarProducto(s)">
          {{ s.nombre }} <span class="text-muted">({{ s.codigo }})</span>
        </li>
      }
    </ul>
  }
    </div>


    <div class="card-body flex-fill">
      </div>

    <div class="card-footer"> ... </div>

    <div class="card-body">
              <div class="btn-group w-100">
                @for (cat of categorias; track cat) {
                  <button 
                    type="button"
                    class="btn" 
                    [class.btn-primary]="categoriaSeleccionada === cat"
                    (click)="seleccionarCategoria(cat)">
                    {{ cat }}
                  </button>
                }
              </div>
            </div>


      <div class="card-body flex-fill">
        <div class="row row-cards">
          @for (p of productosPaginados; track p.id) {
            <div class="col-sm-6 col-md-4">
              <div class="card card-sm card-link card-link-pop" (click)="seleccionarProducto(p)" style="cursor: pointer;">
                <div class="card-body">
                  <div class="d-flex align-items-center">
                    <div>
                      <strong>{{ p.nombre }}</strong>
                      <div class="text-muted small">Stock: {{ p.stock }}</div>
                    </div>
                    <div class="ms-auto">
                      <span class="badge bg-green-lt">${{ p.precio }}</span>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          } @empty {
                  <div class="empty">
                    <div class="empty-icon"><i class="ti ti-package-off"></i></div>
                    <p class="empty-title">No hay productos en el catálogo</p>
                  </div>
                }
              
              
              @if (buscarpro && productos.length === 0) {
                <div class="alert alert-warning mt-3">
                  No se encontraron productos con "{{ buscarpro }}"
                </div>
              }

        </div>
      </div>

      <div class="card-footer d-flex align-items-center bg-transparent border-top">
        <p class="m-0 text-muted d-none d-sm-inline-block">Página {{ paginaActual }}</p>
        <ul class="pagination m-0 ms-auto">
          <li class="page-item" [class.disabled]="paginaActual === 1">
            <button class="page-link" (click)="cambiarPagina(paginaActual - 1)"><i class="ti ti-chevron-left"></i></button>
          </li>
          <li class="page-item active"><span class="page-link">{{ paginaActual }}</span></li>
          <li class="page-item" [class.disabled]="paginaActual === totalPaginas">
            <button class="page-link" (click)="cambiarPagina(paginaActual + 1)"><i class="ti ti-chevron-right"></i></button>
          </li>
        </ul>
      </div>
    </div>
  </div>
<!-- Modal con teclado numérico -->
<div #cantidadModal class="modal fade" id="cantidadModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-sm modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Cantidad</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body text-center">
        <p class="fw-bold">{{ productoSeleccionado?.nombre }}</p>
        <input type="text" [(ngModel)]="cantidadSeleccionada" class="form-control text-center mb-3" min="1" (keydown)="manejarTeclado($event)">

        <!-- Teclado numérico -->
        <div class="row g-2">
          @for (num of [1,2,3,4,5,6,7,8,9]; track num) {
            <div class="col-4">
              <button class="btn btn-secondary w-100" (click)="agregarNumero(num)">  {{ num }} </button>
             </div>
             }
          <div class="col-4">
            <button class="btn btn-secondary w-100" (click)="borrarCantidad()">⌫</button>
          </div>
          <div class="col-4">
            <button class="btn btn-secondary w-100" (click)="agregarNumero(0)">0</button>
          </div>
          <div class="col-4">
            <button class="btn btn-success w-100" (click)="confirmarCantidad()" data-bs-dismiss="modal">✔</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

  <div class="col-lg-4">
    <div class="card" style="position: sticky; top: 20px;"> <div class="card-header bg-primary-lt">
        <h3 class="card-title"><i class="ti ti-shopping-cart me-2"></i>Detalle de Venta</h3>
      </div>
      
      <div class="card-body">
        <div class="mb-3">
          <label class="form-label">Cliente</label>
          <input type="text" class="form-control" [(ngModel)]="cliente" placeholder="Nombre del cliente">
        </div>
      <!--
        <div class="list-group list-group-flush" style="max-height: 350px; overflow-y: auto;">
          @for (item of carrito; track item.id; let i = $index) {
            <div class="list-group-item">
              <div class="row align-items-center">
                <div class="col text-truncate">
                  <span class="text-body d-block">{{ item.nombre }}</span>
                  <small class="text-muted">${{ item.precio }} x {{ item.cantidad }}</small>
                </div>
                //esto es un comentario Input editable para cantidad 
                 <input type="number" 
                 [(ngModel)]="item.cantidad"  
                 (change)="validarStock(item)" 
                 min="1"
                 class="form-control form-control-sm" style="width: 70px;">
                
                <div class="col-auto">
                  <button class="btn btn-danger btn-sm" (click)="eliminarDelCarrito(i)">
                    <i class="ti ti-trash"></i>
                  </button>
                </div>
              </div>
              //esto estaba en comentario @if (item.mensajeStock) { <small class="text-danger">{{ item.mensajeStock }}</small> }
            </div>
          } @empty {
            <div class="text-center py-4 text-muted small">El carrito está vacío</div>
          }
        </div>
      -->

  <div class="card">
   <div class="table-responsive">
    <table class="table table-vcenter card-table table-striped">
      <thead>
        <tr>
          <th>Código</th>
          <th>Descripción</th>
          <th class="w-25">Empaque</th> <th class="text-end">Precio</th>
          <th class="w-1">Cant.</th>
          <th class="text-end">Subtotal</th>
          <th class="w-1"></th>
        </tr>
      </thead>
      <tbody>
        @for (item of carrito; track item.id; let i = $index) {
          <tr>
            <td class="text-muted">{{ item.codigo }}</td>
            <td class="fw-bold">{{ item.nombre }}</td>
            
            <td>
              <select class="form-select form-select-sm" 
                      [(ngModel)]="item.empaqueSeleccionado"
                      (change)="actualizarFila(item)">
                @for (emp of item.empaques; track emp.id) {
                  <option [ngValue]="emp">
                    {{ emp.nombre_empaque }}
                  </option>
                }
              </select>
            </td>

            <td class="text-end">
              {{ item.precio | currency:'symbol':'1.2-2' }}
            </td>
            
            <td>
              <input type="number" 
                 [(ngModel)]="item.cantidad"  
                 (change)="validarStock(item)" 
                 min="1"
                 class="form-control form-control-sm" style="width: 70px;">
            </td>

            <td class="text-end fw-bold">
              {{ (item.precio || 0) * (item.cantidad || 0) | currency }}
            </td>

            <td>
              <button class="btn btn-icon btn-ghost-danger btn-sm" (click)="eliminarDelCarrito(i)">
                <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                  <path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M4 7l16 0" /><path d="M10 11l0 6" /><path d="M14 11l0 6" /><path d="M5 7l1 12a2 2 0 0 0 2 2h8a2 2 0 0 0 2 -2l1 -12" /><path d="M9 7v-3a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v3" />
                </svg>
              </button>
            </td>
          </tr>
        } @empty {
          <tr>
            <td colspan="7" class="text-center py-4">
              <div class="text-muted">No hay productos en la factura</div>
            </td>
          </tr>
        }
      </tbody>
    </table>
  </div>
</div>
      </div>

      <div class="card-footer">
        <div class="d-flex justify-content-between h2 mb-3">
          <div class="d-flex flex-column align-items-end">
  <div class="mb-2">
    <span class="text-muted fw-bold me-2">Subtotal:</span>
    <span class="text-dark fw-bold">
      {{ subtotal | currency:'NIO':'symbol-narrow':'1.2-2' }}
    </span>
  </div>

  <div class="border-top pt-2">
    <span class="h3 mb-0 me-2">Total:</span>
    <span class="h3 mb-0 text-primary">
      {{ total | currency:'NIO':'symbol-narrow':'1.2-2' }}
    </span>
  </div>
</div>
        </div>
        <button class="btn btn-primary w-100" [disabled]="carrito.length === 0 || !cliente" (click)="finalizarVenta()">
          <i class="ti ti-check me-2"></i> Procesar Pago
        </button>
        
        <!-- Spinner -->
         @if (cargando){
          <div  class="d-flex align-items-center mt-3">
            <div class="spinner-border text-primary me-2" role="status">
              <span class="visually-hidden">Procesando...</span>
            </div>
              <strong>Procesando venta...</strong>
          </div>
        }
        <!-- Mensaje de error -->
         @if(errorMsg){
          <div  class="alert alert-danger mt-3" role="alert">
            {{ errorMsg }}
          </div>
        }
        <!-- Contenedor de Toasts (siempre fijo en pantalla) -->
          <div class="position-fixed bottom-0 end-0 p-3" style="z-index: 11">
             <!-- Toast de éxito -->
               <div id="ventaToast" class="toast align-items-center text-bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true">
                 <div class="d-flex"> <div class="toast-body">
                   ✅ Venta exitosa
                 </div>
                  <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Cerrar">
                  </button>
                </div>
          </div>
                  <!-- Toast de error -->
                    <div id="errorToast" class="toast align-items-center text-bg-danger border-0 mt-2" role="alert" aria-live="assertive" aria-atomic="true">
                       <div class="d-flex">
                         <div class="toast-body"> ❌ Error al procesar la venta
                         </div>
                          <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Cerrar">
                          </button>
                        </div>
                     </div>
                    </div>
        <button class="btn-secondary w-100" [disabled]="carrito.length === 0" (click)="generarPDF(0)">
        <i class="ti ti-check me-2"></i> Vista Previa PDF
        </button>
      </div>

    </div>
  </div>

</div>